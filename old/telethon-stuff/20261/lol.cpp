#include <iostream>
#include <bitset>
#include <inttypes.h>
#include <vector>

using namespace std;

int main(void) {

  vector<int64_t> ids(
      (istream_iterator<int64_t>(cin)),
      istream_iterator<int64_t>()
  );

  uint32_t acc = 0;
  for (auto id : ids) {
    
  }

  return 0;
}

